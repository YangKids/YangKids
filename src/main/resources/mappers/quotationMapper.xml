<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yangkids.model.dao.QuotationDao">

	<!-- 전체글 조회 -->
	<select id="selectAll" parameterType="int" resultType="Quotation">
		SELECT Quotation.* FROM Quotation;
	</select>

</mapper>
